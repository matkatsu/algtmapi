# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
*.coverprofile

# Project-local glide cache, RE: https://github.com/Masterminds/glide/issues/736
.glide/
vendor/

# ignore mysql-workbench's backup file
docs/*.bak

# avoid to be mixed config for production or staging
config/*.toml

# ignore precompiled dir or files
assets/
app/**
swagger/**
swaggerassets/swagger.go
!swagger/swaggerui

# do not ignore .gitkeep
!.gitkeep

# should be ignored on mac os
.DS_Store

# ignore binary
goaviron
